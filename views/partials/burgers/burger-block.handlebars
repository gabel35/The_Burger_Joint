<li>
	{{name}}

	<button class="change-eatNow" data-id="{{id}}" data-eatNow="{{eatNow}}">
		{{#if eatNow}}Eat!{{else}}Order Another{{/if}}
	</button>

	<button class="eaten-burger" data-id="{{this.id}}">Throw it in the Trash</button>
</li>
